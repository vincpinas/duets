import { useParams } from "react-router-dom"
import "./Room.scss"

function Room() {
  const { room } = useParams()

  return (
    <div className="c-room -page -bg-special">
        
    </div>
  )
}

export default Room